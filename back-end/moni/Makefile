up: #levantar el contenedor
	docker-compose -f docker-compose.yml up -d

down: #bajar el contenedor
	docker-compose -f docker-compose.yml down

logs: #Ver el log de docker
	docker logs -f moni-backend

bash: #Ingresar a la consola de un contenedor
	docker exec -it moni-back bash

error: /usr/bin/env: �python\r�: No such file or directory
	Esto puede pasar en windows, es un problema con el archivo manage.py